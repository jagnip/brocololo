import { IngredientsPageSkeleton } from "@/components/ingredients/ingredients-page-skeleton";

export default function Loading() {
  // Route-level skeleton for ingredients index and segment transitions.
  return <IngredientsPageSkeleton />;
}
